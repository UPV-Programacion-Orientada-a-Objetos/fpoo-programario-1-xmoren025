package edu.upvictoria.fpoo.Problema17;

import static java.lang.Math.*;

public class Hipotenusa {
    public double calcularHipo(double ca, double cb){
        return (double)  sqrt(pow(ca,2)+pow(cb,2));
    }
}